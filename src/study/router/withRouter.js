import React from "react";
/*
import { withRouter } from "react-router-dom";

const WithRouter_Ex = ({ location, match, history }) => {
  return (
    <div>
      <h1>withRouter 기능 입니다.</h1>
      <h4>Location</h4>
      <textarea
        value={JSON.stringify(location, null, 2)}
        rows={7}
        readOnly={true}
      />
      <h4>match</h4>
      <textarea
        value={JSON.stringify(match, null, 2)}
        rows={7}
        readOnly={true}
      />
      <button onClick={() => history.push("/")}>Home으로...</button>
    </div>
  );
};

export default withRouter(WithRouter_Ex);
*/
